# Copy this file to .env.test and fill in real values.
# .env.test is gitignored — never commit real credentials.

# ── Test user credentials (create this user in Supabase) ──────────────────
TEST_USER_EMAIL=arbiter-test@example.com
TEST_USER_PASSWORD=change-me-to-a-strong-password

# ── Supabase (same project as production, but test user lives in a separate workspace) ──
SUPABASE_URL=https://evjvdfqpsbnpfsmzhpfn.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_8qsrZAPt5oesXlSNBTJjpA_YjY8OqTk

# ── Test workspace ID (create a dedicated workspace for the test user) ────
TEST_WORKSPACE_ID=

# ── Optional overrides ───────────────────────────────────────────────────
# TEST_SERVER_URL=http://localhost:8787
# TEST_LIVE_URL=https://nickporterfieldfullsteam.github.io/arbiter
